#!/usr/bin/env python

from setuptools import setup

setup(
    name='AgroBook',
    description='Cuaderno de campo',
    version='0.0.1',
    author='raulnr',
    author_email='raulnuneztrabajo@gmail.com',
    packages=[
        'rnr'
    ],
    zip_safe=False,
    install_requires=[],
    include_package_data=True,
    setup_requires=[],
    tests_require=[]
    )
